name: 'Terraform'

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    environment: production

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Plan
        run: terraform plan -input=false

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false

      # Adicionando etapa para atualizar a infraestrutura na AWS
      - name: Update AWS Infrastructure
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          # Comandos para atualizar a infraestrutura na AWS, por exemplo:
          # aws cloudformation deploy --template-file my-stack.yaml --stack-name MyStack --capabilities CAPABILITY_IAM
