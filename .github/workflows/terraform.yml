name: 'Terraform'

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    environment: production

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Plan
        run: terraform plan -input=false

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false

      # Adicione etapas adicionais aqui para atualizar a infraestrutura na AWS após a aplicação de mudanças com o Terraform
      - name: Update AWS Infrastructure
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          # Comandos para atualizar a infraestrutura na AWS
          # Por exemplo, você pode usar aws cli commands para atualizar um serviço ou implantar uma nova instância EC2
          # Exemplo:
          # aws ec2 run-instances --image-id ami-123456 --count 1 --instance-type t2.micro
